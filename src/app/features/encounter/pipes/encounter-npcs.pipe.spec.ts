import { EncounterNpcsPipe } from './encounter-npcs.pipe';

describe('EncounterNpcsPipe', () => {
  it('create an instance', () => {
    const pipe = new EncounterNpcsPipe();
    expect(pipe).toBeTruthy();
  });
});
